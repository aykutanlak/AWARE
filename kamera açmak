import cv2

kamera = cv2.VideoCapture(0)  

while True:
    ret, görüntü = kamera.read()  
    flipped2 = cv2.flip(görüntü, 1)
    cv2.imshow("Kamera", flipped2)  

    if cv2.waitKey(1) == ord('q'):
        break

kamera.release()  # Kamerayı kapat
cv2.destroyAllWindows()  # Pencereleri kapat
